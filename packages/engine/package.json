{
  "name": "@midimaker/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Pure TypeScript MIDI generation engine (no Node/Electron/DOM dependencies) - portable to a future JUCE VST3 plugin",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
